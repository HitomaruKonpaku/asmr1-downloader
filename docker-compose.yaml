---
services:
  asmr1-downloader:
    container_name: asmr1-downloader
    image: ghcr.io/hitomarukonpaku/asmr1-downloader
    restart: unless-stopped
    ports:
      - 3000:3000
    environment:
      DOWNLOAD_PARALLEL: 2
      DOWNLOAD_TIMEOUT: 120000
    volumes:
      - ./download:/download
